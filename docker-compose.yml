version: '3.8'

services:
  whatsapp-api:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - PORT=3000
      - API_KEY=${API_KEY}
      - WEBHOOK_URL=${WEBHOOK_URL}
    volumes:
      - ./sessions:/app/sessions
    restart: always
